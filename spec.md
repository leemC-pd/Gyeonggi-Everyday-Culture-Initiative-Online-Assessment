# 2026 경기도 생활문화 사업 온라인평가시스템 — 명세서

> 작성일: 2026-07-02 갱신  
> 반영: 최종 진단지(260702), 채점 규칙 확정안, 진입/성장 단계 엑셀 업로드 워크플로우  
> 주요 변경: §5 문항 수 명시·행동앵커 미포함 명시 / §6 배점·매핑·(성장) 처리·결측·조정 플래그 구체화 / §7 신규(엑셀 업로드) / §8 `stage_upload_logs` 추가 / §13 남은 작업 재정리

---

## 1. 개요

경기도 생활문화 사업의 평가를 온라인으로 진행하기 위한 웹 시스템.  
관리자가 평가 대상을 등록하고 위원을 배정하면, 평가위원이 각자 온라인으로 진단지를 작성·제출한다.  
관리자는 위원들의 점수를 확인하고 합의점수를 별도로 입력할 수 있다.

---

## 2. 기술 스택

| 항목 | 내용 |
|------|------|
| 프레임워크 | Next.js 16.2.9 (App Router, Turbopack) |
| DB / Auth | Supabase (PostgreSQL + Auth) |
| 스타일 | Tailwind CSS |
| 언어 | TypeScript |
| 배포 환경 | Vercel 또는 로컬 (`npm run dev`) |

---

## 3. 사용자 역할

| 역할 | 설명 |
|------|------|
| `admin` | 관리자. 전체 기능 접근 가능 |
| `evaluator` | 평가위원. 본인에게 배정된 대상만 평가 |

---

## 4. 인증

- **방식**: 이메일 + 비밀번호 (Supabase Auth)
- **위원 등록**: 관리자가 이름·이메일·임시 비밀번호를 직접 입력해 생성
- **비밀번호 변경**: Supabase 대시보드(Users 탭)에서 관리자가 수동 변경
- **로그아웃**: 관리자·위원 화면 모두 상단에 로그아웃 버튼 제공

---

## 5. 진단지 종류

| ID | 이름 | 문항 수 | 단계 |
|----|------|-------|------|
| `platform_foundation` | 플랫폼 사업 — 기초재단 | 27 | 진입 / 성장 (§7 관리자 결정) |
| `platform_org` | 플랫폼 사업 — 유관기관·단체 | 25 | 진입 / 성장 (§7 관리자 결정) |
| `private_space_foundation` | 민간공간 — 기초재단 | 21 | 단일 가중 (단계 구분 없음) |

> **민간공간**: 재단 담당자 사전조사(구글폼)만 수집하고 공간 운영자 대상 설문은 실시하지 않는다. 위원은 사전조사 결과와 인터뷰를 근거로 직접 채점한다. 참여이력(신규/연속 n년차)은 대상 등록 시 참고 필드로 저장하되 채점에 영향을 주지 않는다.  
> **행동앵커 미포함**: 다양성·개방성 문항의 행동앵커는 최종안에서 제외되었다. 관련 UI/DB 필드가 남아 있으면 제거한다. 위원 간 편차는 §6-8 조정 로직으로 관리한다.  
> **특정-공동체 지침**: `target_model`이 `특정-공동체형`·`혼합형`인 경우 다양성·개방성 영역 상단에 위원 지침을 노출한다 (§12 참조).

---

## 6. 채점 방식

### 6-1. 문항 척도

- 7점 리커트 척도 (화면 표시 순서: 7 → 1)

### 6-2. 문항 환산

```
환산점수 = (원점수 − 1) ÷ 6 × 100
```
- 7점 → 100점, 4점 → 50점, 1점 → 0점

### 6-3. 영역 점수

```
영역평균 = 해당 영역 응답 문항 환산점수의 산술평균
영역기여점수 = 영역평균 × (영역 배점 ÷ 100)
```
- N/A(비적용) 문항은 영역평균 분모에서 제외 (§6-6, §6-7 참조)

### 6-4. 총점 및 등급

```
총점 = 모든 영역기여점수 합산 (만점 100)
```

| 등급 | 기준 |
|------|------|
| A | 85점 이상 |
| B | 75점 이상 85점 미만 |
| C | 75점 미만 |

- 등급 판정은 반올림 전 원값 기준.  
- 화면·보고서 표기는 소수 첫째 자리 반올림.

> 이전 스펙의 "85점 초과 / 75점 초과" 표기를 "이상"으로 통일. 2025 보고서와 일관.

### 6-5. 영역 배점

| 도구 / 단계 | 적합성 | 주체성 | 다양성 | 개방성 | 효과성 | 확장성 | 체계성 |
|------------|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| `platform_foundation` — 진입 | 20 | 20 | 10 | 10 | 20 | 10 | 10 |
| `platform_foundation` — 성장 | 10 | 20 | 10 | 10 | 15 | 15 | 20 |
| `platform_org` — 진입 | 20 | 20 | 10 | 10 | 20 | 10 | 10 |
| `platform_org` — 성장 | 10 | 20 | 10 | 10 | 15 | 15 | 20 |
| `private_space_foundation` — 단일 | 20 | 20 | 10 | 10 | 20 | 10 | 10 |

### 6-6. 문항–영역 매핑 및 (성장) 문항 처리

#### `platform_foundation` (27문항)

| 영역 | 문항 번호 | (성장) 전용 |
|-----|-----------|:---------:|
| 적합성 | 1 ~ 4 | 4 |
| 주체성 | 5 ~ 8 | — |
| 다양성 | 9 ~ 12 | — |
| 개방성 | 13 ~ 15 | — |
| 효과성 | 16 ~ 19 (19: 종합) | — |
| 확장성 | 20 ~ 23 | — |
| 체계성 | 24 ~ 27 | 26, 27 |

#### `platform_org` (25문항)

| 영역 | 문항 번호 | (성장) 전용 |
|-----|-----------|:---------:|
| 적합성 | 1 ~ 3 | 3 |
| 주체성 | 4 ~ 6 | — |
| 다양성 | 7 ~ 10 | — |
| 개방성 | 11 ~ 13 | — |
| 효과성 | 14 ~ 17 (17: 종합) | — |
| 확장성 | 18 ~ 21 | — |
| 체계성 | 22 ~ 25 | 24, 25 |

#### `private_space_foundation` (21문항)

| 영역 | 문항 번호 |
|-----|-----------|
| 적합성 | 1 ~ 3 |
| 주체성 | 4 ~ 6 |
| 다양성 | 7 ~ 9 |
| 개방성 | 10 ~ 12 |
| 효과성 | 13 ~ 16 (16: 종합) |
| 확장성 | 17 ~ 18 |
| 체계성 | 19 ~ 21 |

**(성장) 문항 처리 규칙**

- 대상 단계가 `진입`이면 (성장) 표시 문항을 UI에서 자동 비활성화(N/A)하고 영역평균 계산에서 제외한다.  
- 대상 단계가 `성장`이면 모든 문항 필수.  
- `private_space_foundation`은 (성장) 전용 문항이 없어 항상 전 문항 필수 (§6-7의 20번 예외 제외).

### 6-7. 결측·무응답

- 문항 무응답 시 해당 문항을 영역평균의 분모에서 제외한다(0점 처리 금지).  
- 한 영역의 모든 문항이 무응답이면 총점 산출 불가로 처리하고 제출 시 오류로 차단한다.  
- **`private_space_foundation` 20번**(연속 지원 공간의 활동 고도화)은 신규 공간만 지원한 재단에 한해 위원이 N/A를 선택할 수 있다. 폼에서 "해당없음" 체크박스를 제공한다.

### 6-8. 복수 위원 점수 통합 및 조정 플래그

- **통합 단위**: 문항 단위 통합. 문항별로 위원 점수를 평균한 뒤 §6-2~6-4 산식을 적용한다.  
- **편차 플래그**: 동일 문항에서 위원 간 원점수 차이(max − min)가 2점 이상이면 시스템이 해당 문항을 조정 대상으로 자동 표시한다.  
- **우선 감시 영역**: 다양성·개방성 문항은 편차 플래그 UI에서 시각적으로 강조 (행동앵커 미적용 보완).  
- **합의점수**: 조정 결과는 위원별 원점수를 보존한 채 관리자가 `reconciliations`에 별도 입력한다. 합의점수의 총점·등급은 §6-2~6-4 산식으로 자동 계산.

---

## 7. 대상 단계 결정 및 엑셀 업로드 (신규)

### 7-1. 원칙

- 진입/성장 단계는 관리자가 결정하고 엑셀로 일괄 업로드한다.  
- 판정 기준은 참여이력을 우선하되(예: 신규 → 진입, 연속 3년차 이상 → 성장), 경계 사례(연속 2년차 등)는 관리자가 사전조사와 이력을 검토해 결정한다.  
- 사전조사 자기분류(진입/성장/확산)와 관리자 판정이 다르면 관리자 판정을 우선한다. 자기분류의 `확산`은 채점상 `성장`으로 취급한다.  
- `private_space_foundation`은 단계 구분이 없으므로 업로드 대상에서 제외되거나 `stage` 컬럼을 공란으로 둔다.

### 7-2. 엑셀 포맷

| 컬럼 | 필수 | 설명 |
|------|:---:|------|
| `subject_id` | ● | 시스템에 등록된 평가 대상 ID |
| `subject_name` | ● | 재단·단체명 + 사업명 (매칭 확인용, 시스템 값과 일치해야 함) |
| `instrument` | ● | `platform_foundation` / `platform_org` / `private_space_foundation` |
| `stage` | 조건부 | 플랫폼 도구에서는 `진입` 또는 `성장` 필수. 민간공간은 공란 허용. |
| `history_note` | ○ | 참여이력 메모 (예: `연속 3년차`). 판정 근거 참고용. |
| `admin_note` | ○ | 관리자 메모 |

### 7-3. 업로드 절차 및 잠금

- 관리자는 `/admin/subjects/upload-stage`에서 엑셀 파일을 업로드한다.  
- 업로드 시 검증:
  - `subject_id` 미등록 → 오류
  - `instrument` 값 불일치(시스템 등록값과 다름) → 오류
  - 플랫폼 도구인데 `stage` 누락 → 오류
  - `stage` 값이 `진입`·`성장` 외 → 오류
- 성공 시 대상별 `subjects.stage`가 갱신되고, 변경 내역이 `stage_upload_logs`에 기록된다.  
- **잠금**: 해당 대상에 대해 위원이 임시저장 또는 제출을 시작한 이후에는 관리자 확인 다이얼로그를 거쳐야만 재업로드 가능하다. 변경 시 해당 대상 담당 위원에게 시스템 알림을 발송한다.

---

## 8. DB 테이블

| 테이블 | 설명 |
|--------|------|
| `profiles` | 사용자 정보 (role, name, email) |
| `subjects` | 평가 대상 기관 (instrument, stage, fiscal_year, target_model, history_note) |
| `stage_upload_logs` | (신규) 단계 엑셀 업로드 이력 (uploaded_at, admin_id, filename, changes jsonb) |
| `assignments` | 위원-대상 배정 |
| `evaluations` | 평가 (status: draft/submitted, area_scores, total_score, grade) |
| `responses` | 문항별 응답 (item_code, score, qualitative, is_na boolean) |
| `reconciliations` | 관리자 합의점수 (area_scores, total_score, grade, reason) |
| `instrument_overrides` | 진단지 문항 커스터마이징 (jsonb) |

> `responses.is_na`: N/A 처리 표시. 진입 단계에서 (성장) 문항이 자동 N/A 되는 경우와 위원이 직접 N/A 선택한 경우(민간공간 20번) 모두 이 필드로 관리.  
> RLS 비활성화 상태. 인증·권한 체크는 Next.js 서버 코드에서 처리.  
> 모든 서버사이드 DB 접근은 service role client 사용.

---

## 9. 화면 구성

### 9-1. 공통

- `/login` — 이메일 + 비밀번호 로그인

### 9-2. 평가위원

| 경로 | 설명 |
|------|------|
| `/` | 배정된 평가 대상 목록 (상태: 미시작/임시저장/제출완료, 총점·등급 표시) |
| `/evaluate/[assignmentId]` | 평가 폼 (문항 응답 + 영역별 정성 입력, 대상 단계에 따라 (성장) 문항 자동 비활성화) |
| `/preview/platform_foundation` | 진단지 미리보기 (재단) |
| `/preview/platform_org` | 진단지 미리보기 (단체) |

### 9-3. 관리자 (`/admin`)

| 경로 | 설명 |
|------|------|
| `/admin` | 대시보드 (전체 현황 요약) |
| `/admin/subjects` | 평가 대상 목록 |
| `/admin/subjects/new` | 평가 대상 등록 |
| `/admin/subjects/upload-stage` | (신규) 진입/성장 단계 엑셀 일괄 업로드 (§7) |
| `/admin/subjects/[id]` | 대상 상세: 위원 배정·취소, 성장단계 수정, 합의점수 입력, 위원별 점수 비교(편차 ≥2점 문항 자동 강조) |
| `/admin/evaluators` | 평가위원 목록 + 등록 |
| `/admin/progress` | 전체 진행 현황 |
| `/admin/export` | 점수 내보내기 (xlsx, docx) |
| `/admin/instruments` | 진단지 편집 목록 |
| `/admin/instruments/[id]` | 진단지 문항 편집 (추가·삭제·문구 수정·적용단계 변경) |
| `/preview/platform_foundation` | 진단지 미리보기 (재단) |
| `/preview/platform_org` | 진단지 미리보기 (단체) |

---

## 10. 평가 흐름

1. 관리자가 평가 대상 등록 (기관명, 진단지 종류, 대상모델, 비고)
2. **관리자가 진입/성장 단계 엑셀 업로드** (§7) ← 신규
3. 관리자가 평가위원 등록 (이름, 이메일, 임시 비밀번호)
4. 관리자가 대상별로 평가위원 배정
5. 평가위원이 로그인 → 배정된 목록 확인 → 평가 폼 진입
6. 위원이 문항별 척도 선택 + 영역별 정성 입력 → **임시저장** 가능  
   (진입 단계 대상의 (성장) 문항은 자동 비활성화, 민간공간 20번은 위원 선택으로 N/A 가능)
7. 위원이 **제출** → 채점 자동 계산, 이후 수정 불가
8. 관리자가 위원별 점수 비교 확인 (편차 ≥2점 문항 자동 강조)
9. 관리자가 필요 시 **합의점수** 입력 (영역별 점수 직접 입력, 총점·등급 자동 계산)
10. 내보내기 (xlsx/docx)

---

## 11. 진단지 문항 커스터마이징

- 기본 진단지는 JSON 파일(`/lib/instruments.ts`)에 하드코딩
- 관리자가 수정하면 `instrument_overrides` 테이블에 저장
- 조회 시 기본값에 DB 오버라이드를 병합해 제공
- 원본으로 초기화 시 override 레코드 삭제

---

## 12. 대상모델 (target_model)

| 값 | 설명 |
|----|------|
| `개방-일반형` | 기본. 다양성·개방성 문항을 표준 해석으로 진단. |
| `특정-공동체형` | 다양성·개방성 영역 상단에 위원 지침 노출 (기준점 조정: 대상 내 다양성으로 읽기, 정보 개방/참여 개방 분리, 프라이버시 원칙). |
| `혼합형` | 동일한 지침 노출. 안전 경계 활동과 개방 활동을 나누어 진단하고 하나의 점수로 종합하도록 안내. |

> 척도와 산식은 target_model에 관계없이 동일. 지침은 문항 상단 안내 문구로만 노출.

---

## 13. 남은 작업 (미구현)

### 핵심 4개 기능

- [ ] **진입/성장 단계 엑셀 업로드**: 관리자 업로드 화면, 검증, 잠금·재업로드 흐름, `stage_upload_logs` (§7)
- [ ] **(성장) 문항 자동 비활성화**: 대상 단계 기준 폼 렌더링, `is_na` 처리, 영역평균 산식 반영 (§6-6, §6-7)
- [ ] **위원 간 편차 자동 플래그**: 문항별 원점수 차이 ≥2점 시 강조 UI, 다양성·개방성 우선 감시 (§6-8)
- [ ] **민간공간 20번 N/A 선택 UI**: 위원 폼에서 "해당없음" 체크박스 및 산식 반영 (§6-7)

### 정리·데이터 작업

- [ ] 진단지 최종본(260702) 반영 및 오탈자 수정 (별첨 문서 `2026_평가_채점규칙_및_진단지수정사항.docx` §4 참조)
- [ ] 행동앵커 관련 폼·데이터 필드 제거 (남아 있는 경우)
- [ ] 등급 기준 "이상" 표기 및 산식 반올림 처리 정리 (§6-4)
- [ ] `platform_foundation`/`platform_org` 배점 확정값(§6-5) 반영, 이전 임시값 정리
- [ ] `private_space_foundation` 이전 논의값(적합성 15·주체성 15)이 코드에 남아 있으면 확정값(20·20)으로 교체
- [ ] 실제 평가 대상 기관 전체 데이터 입력
- [ ] 실제 평가위원 전체 등록 및 배정
- [ ] 내보내기 기능 테스트 (xlsx/docx 정상 출력 확인)
- [ ] 진행상황 페이지 내용 확인
- [ ] 전체 평가 흐름 end-to-end 테스트 (제출·합의점수·내보내기까지)
