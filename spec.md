# 2026 경기도 생활문화 사업 온라인평가시스템 — 명세서

> 작성일: 2026-07-02 기준 구현 상태

---

## 1. 개요

경기도 생활문화 사업의 평가를 온라인으로 진행하기 위한 웹 시스템.  
관리자가 평가 대상을 등록하고 위원을 배정하면, 평가위원이 각자 온라인으로 진단지를 작성·제출한다.  
관리자는 위원들의 점수를 확인하고 합의점수를 별도로 입력할 수 있다.

---

## 2. 기술 스택

| 항목 | 내용 |
|------|------|
| 프레임워크 | Next.js 16.2.9 (App Router, Turbopack) |
| DB / Auth | Supabase (PostgreSQL + Auth) |
| 스타일 | Tailwind CSS |
| 언어 | TypeScript |
| 배포 환경 | Vercel 또는 로컬 (`npm run dev`) |

---

## 3. 사용자 역할

| 역할 | 설명 |
|------|------|
| `admin` | 관리자. 전체 기능 접근 가능 |
| `evaluator` | 평가위원. 본인에게 배정된 대상만 평가 |

---

## 4. 인증

- **방식**: 이메일 + 비밀번호 (Supabase Auth)
- **위원 등록**: 관리자가 이름·이메일·임시 비밀번호를 직접 입력해 생성
- **비밀번호 변경**: Supabase 대시보드(Users 탭)에서 관리자가 수동 변경
- **로그아웃**: 관리자·위원 화면 모두 상단에 로그아웃 버튼 제공

---

## 5. 진단지 종류

| ID | 이름 | 성장단계 |
|----|------|----------|
| `platform_foundation` | 플랫폼 사업 — 기초재단 | 진입 / 성장 |
| `platform_org` | 플랫폼 사업 — 유관기관·단체 | 진입 / 성장 |
| `private_space_foundation` | 민간공간 — 기초재단 | 신규 / 연속 (채점 미반영) |

> **민간공간**: 재단 담당자 의견은 별도 구글 설문으로 수집하며, 평가위원이 그 결과를 참고해 직접 점수를 매긴다. 시스템 내 민간공간 자체 응답 수집 기능은 사용하지 않는다.

---

## 6. 채점 방식

### 6-1. 문항 척도

- 7점 리커트 척도 (화면 표시 순서: 7 → 1)

### 6-2. 문항 환산

```
환산점수 = (원점수 − 1) ÷ 6 × 100
```
- 7점 → 100점, 4점 → 50점, 1점 → 0점

### 6-3. 영역 점수

```
영역평균 = 해당 영역 문항 환산점수의 산술평균
영역기여점수 = 영역평균 × (영역 배점 ÷ 100)
```

### 6-4. 총점 및 등급

```
총점 = 모든 영역기여점수 합산 (만점 100)
```

| 등급 | 기준 |
|------|------|
| A | 85점 초과 |
| B | 75점 초과 |
| C | 그 외 |

### 6-5. 영역 배점 (성장단계별 적용)

- `platform_foundation`, `platform_org`: 성장단계('진입'/'성장')에 따라 영역 배점이 달라짐
- `private_space_foundation`: 단일 배점 ('all' 키)

---

## 7. DB 테이블

| 테이블 | 설명 |
|--------|------|
| `profiles` | 사용자 정보 (role, name, email) |
| `subjects` | 평가 대상 기관 (instrument, stage, fiscal_year, target_model) |
| `assignments` | 위원-대상 배정 |
| `evaluations` | 평가 (status: draft/submitted, area_scores, total_score, grade) |
| `responses` | 문항별 응답 (item_code, score, qualitative) |
| `reconciliations` | 관리자 합의점수 (area_scores, total_score, grade, reason) |
| `instrument_overrides` | 진단지 문항 커스터마이징 (jsonb) |

> RLS 비활성화 상태. 인증·권한 체크는 Next.js 서버 코드에서 처리.  
> 모든 서버사이드 DB 접근은 service role client 사용.

---

## 8. 화면 구성

### 8-1. 공통

- `/login` — 이메일 + 비밀번호 로그인

### 8-2. 평가위원

| 경로 | 설명 |
|------|------|
| `/` | 배정된 평가 대상 목록 (상태: 미시작/임시저장/제출완료, 총점·등급 표시) |
| `/evaluate/[assignmentId]` | 평가 폼 (문항 응답 + 영역별 정성 입력) |
| `/preview/platform_foundation` | 진단지 미리보기 (재단) |
| `/preview/platform_org` | 진단지 미리보기 (단체) |

### 8-3. 관리자 (`/admin`)

| 경로 | 설명 |
|------|------|
| `/admin` | 대시보드 (전체 현황 요약) |
| `/admin/subjects` | 평가 대상 목록 |
| `/admin/subjects/new` | 평가 대상 등록 |
| `/admin/subjects/[id]` | 대상 상세: 위원 배정·취소, 성장단계 수정, 합의점수 입력, 위원별 점수 비교 |
| `/admin/evaluators` | 평가위원 목록 + 등록 (이름·이메일·임시 비밀번호) |
| `/admin/progress` | 전체 진행 현황 |
| `/admin/export` | 점수 내보내기 (xlsx, docx) |
| `/admin/instruments` | 진단지 편집 목록 |
| `/admin/instruments/[id]` | 진단지 문항 편집 (추가·삭제·문구 수정·적용단계 변경) |
| `/preview/platform_foundation` | 진단지 미리보기 (재단) |
| `/preview/platform_org` | 진단지 미리보기 (단체) |

---

## 9. 평가 흐름

1. 관리자가 평가 대상 등록 (기관명, 진단지 종류, 성장단계, 대상모델, 비고)
2. 관리자가 평가위원 등록 (이름, 이메일, 임시 비밀번호)
3. 관리자가 대상별로 평가위원 배정
4. 평가위원이 로그인 → 배정된 목록 확인 → 평가 폼 진입
5. 위원이 문항별 척도 선택 + 영역별 정성 입력 → **임시저장** 가능
6. 위원이 **제출** → 채점 자동 계산, 이후 수정 불가
7. 관리자가 위원별 점수 비교 확인
8. 관리자가 필요 시 **합의점수** 입력 (영역별 점수 직접 입력, 총점·등급 자동 계산)
9. 내보내기 (xlsx/docx)

---

## 10. 진단지 문항 커스터마이징

- 기본 진단지는 JSON 파일(`/lib/instruments.ts`)에 하드코딩
- 관리자가 수정하면 `instrument_overrides` 테이블에 저장
- 조회 시 기본값에 DB 오버라이드를 병합해 제공
- 원본으로 초기화 시 override 레코드 삭제

---

## 11. 대상모델 (target_model)

| 값 | 설명 |
|----|------|
| `개방-일반형` | 기본 |
| `특정-공동체형` | 다양성·개방성 영역에 위원 지침 표시 |
| `혼합형` | 동일 |

---

## 12. 남은 작업 (미구현)

- [ ] 실제 평가 대상 기관 전체 데이터 입력
- [ ] 실제 평가위원 전체 등록 및 배정
- [ ] 내보내기 기능 테스트 (xlsx/docx 정상 출력 확인)
- [ ] 진행상황 페이지 내용 확인
- [ ] 전체 평가 흐름 end-to-end 테스트 (제출까지)
