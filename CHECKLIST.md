# 최종 검토 체크리스트 — 2026 생활문화 온라인평가시스템

> 목적: 실제 평가 개시 전 관리자가 직접 확인할 항목과 방법 정리
> 최종 갱신: 2026-07-02

---

## 0. 사전 준비 (1회)

### 0-1. Supabase SQL 실행
Supabase 대시보드 → SQL Editor에서 아래를 실행했는지 확인.

```sql
-- 응답 N/A 표시 (공간활성화 20번 등)
ALTER TABLE responses ADD COLUMN IF NOT EXISTS is_na boolean NOT NULL DEFAULT false;

-- 인터뷰 정보 (위원 입력)
ALTER TABLE evaluations
  ADD COLUMN IF NOT EXISTS interview_target text,
  ADD COLUMN IF NOT EXISTS interview_datetime text,
  ADD COLUMN IF NOT EXISTS interview_place text;

-- 활동유형·이력 (관리자 업로드)
ALTER TABLE subjects
  ADD COLUMN IF NOT EXISTS activity_type text,
  ADD COLUMN IF NOT EXISTS history text;

-- 단계 업로드 이력 로그
CREATE TABLE IF NOT EXISTS stage_upload_logs (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  admin_id uuid, filename text, changes jsonb,
  created_at timestamptz NOT NULL DEFAULT now());
ALTER TABLE stage_upload_logs DISABLE ROW LEVEL SECURITY;
```

- [ ] 위 SQL 4블록 모두 실행 완료
- [ ] 최신 코드 반영: `git pull` 후 `npm run dev` (또는 배포) 재시작

---

## 1. 진단지 내용 검토 (미리보기)

`관리자 상단 메뉴 → 미리보기(플랫폼·재단 / 플랫폼·단체 / 공간활성화)`

| 확인 항목 | 방법 | ✓ |
|-----------|------|---|
| 문항 수 | 재단 27 / 단체 25 / 공간활성화 21 | ☐ |
| 문항 문구 | 최종 진단지(260702)와 대조, 오탈자 없음 | ☐ |
| 척도 방향 | ⑦(매우 우수) → ①(매우 미흡) 순서로 표시 | ☐ |
| 배점 표시 | 성장단계 탭(진입/성장) 클릭 시 배점 변경 확인 | ☐ |
| (성장) 문항 | 재단 4·26·27, 단체 3·24·25 → 진입 탭에서 "해당없음(자동 제외)" | ☐ |
| C 다양성 안내문 | 플랫폼 2종엔 표시, 공간활성화엔 미표시 | ☐ |
| E 효과성 안내문 | 세 진단지 모두 "E4 항목에서 종합..." 표시 | ☐ |
| 공간활성화 20번 | "해당없음" 체크박스 표시 | ☐ |

---

## 2. 평가 대상 등록 (엑셀 업로드)

`관리자 → 평가대상 → 엑셀 업로드`

**엑셀(종합표 시트) 컬럼 규칙**

| 열 | 항목 | 값 예시 |
|----|------|---------|
| B | 신청단체(기관명) | 고양문화재단 |
| C | 사업명 | 고양, 생활문화플랫폼 |
| D | 지역 | 고양 |
| E | 진단유형 | 플랫폼 / 민간공간 |
| F | 주체유형 | 기초재단 / (단체) |
| G | 활동유형 | 재단: 플랫폼형·사업형·지원형 / 단체: 만남형·제작형·축제형·공간형 |
| H | 성장단계 | 진입 / 성장 (공간활성화는 신규/연속) |
| J | 지원금액 | 숫자 |
| K | 이력 | 신규 / 연속 2년차 / 다회 |

- [ ] 업로드 후 "N건 신규 등록, N건 업데이트" 메시지 확인
- [ ] 평가대상 목록에 기관·사업명·진단지·단계 정상 표시
- [ ] 대상 상세 헤더에 활동유형·이력 표시
- [ ] **주의**: 재업로드는 삭제 아님(덮어쓰기+추가). 기관명 철자 바뀌면 중복 등록됨

---

## 3. 진입/성장 단계 확정

방법 2가지 — 편한 쪽 사용.

- **개별**: `대상 상세 → 단계 옆 ✎ 버튼`으로 진입/성장 변경
- **일괄**: `평가대상 → 단계 업로드` (A=대상명, B=진단지(선택), C=단계)

- [ ] 모든 플랫폼 대상에 진입/성장 지정 완료 (미설정 대상 없음)
- [ ] 단계 업로드 시 결과표에서 오류 행 없음 확인

---

## 4. 평가위원 등록·배정

`관리자 → 평가위원`

- [ ] 위원 등록: 이름·이메일·임시 비밀번호 입력
- [ ] 위원에게 접속주소·이메일·임시 비밀번호 전달 (메일 자동발송 아님)
- [ ] `대상 상세 → 위원 배정`에서 대상별 담당 위원 지정
- [ ] 한 대상에 복수 위원 배정 시 편차 비교 가능

---

## 5. 평가 진행 (위원 시점 점검)

테스트 계정으로 위원 로그인 후 확인.

| 확인 항목 | 방법 | ✓ |
|-----------|------|---|
| 로그인 | 이메일+비밀번호 | ☐ |
| 배정 목록 | 본인 배정 대상만 표시 | ☐ |
| 인터뷰 정보 | 대상/일시/장소 입력·저장 | ☐ |
| 척도 입력 | 문항별 ①~⑦ 선택 | ☐ |
| 영역 정성 | 영역별 정성 평가 입력 | ☐ |
| 실시간 점수 | 영역점수 미리보기 갱신 | ☐ |
| 임시저장 | 저장 후 재접속 시 값 유지 | ☐ |
| 제출 | 제출 후 수정 불가, 총점·등급 표시 | ☐ |
| 공간활성화 20번 | 해당없음 체크 시 채점 제외 | ☐ |

---

## 6. 채점·집계 검토 (관리자)

`대상 상세`

- [ ] 위원별 영역 점수·총점·등급 표 정상
- [ ] **편차 플래그**: 위원 2명 이상 제출 시, 원점수 차 ≥2점 문항 강조 (다양성·개방성 빨강 "우선")
- [ ] **등급 기준**: A ≥85 / B ≥75 / C <75 (반올림 전 원값)
- [ ] **합의점수**: 영역별 점수 직접 입력 → 총점·등급 자동 계산·저장

---

## 7. 내보내기

`관리자 → 내보내기`

- [ ] 점수 xlsx 다운로드 → 대상·위원·영역점수·총점·등급·합의점수 정상
- [ ] 정성 docx 다운로드 → 영역별 정성 평가 취합 정상
- [ ] 등급컷 표기 "A≥85, B≥75" 확인

---

## 8. 알아둘 제약 (설계상 정상 동작)

- 위원 등록·배정 시 **이메일 자동발송 없음** → 접속정보 수동 전달
- 비밀번호 변경은 **Supabase 대시보드(Users)** 에서 관리자가 처리
- 단계 변경 시 **총점 소급 재계산** (제출 평가도 새 배점 반영)
- 엑셀 재업로드는 **삭제가 아니라 덮어쓰기+추가**
- 단계 업로드 후 **위원 재업로드 잠금·알림 기능은 미구현**
- 공간활성화(민간공간) 운영자 설문·교차검증 화면은 **사용 안 함**

---

## 8-1. 테스트 데이터 초기화 (실제 개시 전)

연구원 테스트 후, 실제 평가 시작 전에 Supabase SQL Editor에서 실행.

```sql
-- (권장) 점수·정성·합의점수만 초기화 (대상·위원·진단지 유지)
TRUNCATE responses, evaluations, reconciliations RESTART IDENTITY;

-- 위원 배정까지 초기화하려면
TRUNCATE responses, evaluations, reconciliations, assignments RESTART IDENTITY;

-- 평가대상까지 싹 비우고 새로 업로드하려면
TRUNCATE responses, evaluations, reconciliations, assignments, stage_upload_logs RESTART IDENTITY;
DELETE FROM subjects;

-- 테스트 위원 profiles 삭제 (Auth 계정은 대시보드 Authentication→Users에서 별도 삭제)
DELETE FROM profiles WHERE role = 'evaluator';
```

---

## 9. 미구현/추후 (참고)

- [ ] 단계 재업로드 잠금 + 위원 알림 (spec §7-3)
- [ ] 진행상황 페이지 상세 점검
- [ ] 인터뷰 정보 내보내기 포함 여부 결정
